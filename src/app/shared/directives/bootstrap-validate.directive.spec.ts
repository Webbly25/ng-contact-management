import { BootstrapValidateDirective } from './bootstrap-validate.directive';

describe('BoostrapValidateDirective', () => {
	it('should create an instance', () => {
		const directive = new BootstrapValidateDirective();
		expect(directive).toBeTruthy();
	});
});
